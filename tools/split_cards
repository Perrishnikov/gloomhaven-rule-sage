#!/usr/bin/env bash
set -euo pipefail

DIR="$(cd "$(dirname "$0")" && pwd)"
PY="$DIR/.venv/bin/python"
if [ -x "$PY" ]; then
  exec "$PY" "$DIR/split_cards.py" "$@"
else
  exec /usr/bin/env python3 "$DIR/split_cards.py" "$@"
fi

