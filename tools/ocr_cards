#!/usr/bin/env bash
set -euo pipefail

DIR="$(cd "$(dirname "$0")" && pwd)"
PY="$DIR/.venv/bin/python"
if [ -x "$PY" ]; then
  exec "$PY" "$DIR/ocr_cards.py" "$@"
else
  exec /usr/bin/env python3 "$DIR/ocr_cards.py" "$@"
fi

